/**
 * Ambient module declaration for `docusign-esign`.
 * The package ships without built-in TypeScript types and no
 * DefinitelyTyped `@types/docusign-esign` package exists.
 *
 * This declaration silences TS7016 while still allowing the SDK
 * to be imported and used dynamically.
 */
declare module "docusign-esign";
