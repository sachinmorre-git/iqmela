/**
 * Re-export from shared component for backward compatibility.
 * All sidebar items across the app now use the unified SidebarNavItem.
 */
export { SidebarNavItem, SidebarSection } from "@/components/SidebarNavItem";
