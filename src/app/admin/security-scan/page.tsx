import SecurityScanClient from "./SecurityScanClient";

export const metadata = {
  title: "Security Scanner | IQMela Admin",
};

export default function SecurityScanPage() {
  return <SecurityScanClient />;
}
