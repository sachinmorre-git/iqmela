import SecurityTabsWrapper from "./SecurityTabsWrapper";

export const metadata = {
  title: "Security & Blocks | IQMela Admin",
};

export default function SecurityPage() {
  return <SecurityTabsWrapper />;
}
